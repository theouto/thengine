# Approach

The approach to rendering that I want to take (keyword want) is clustered forward+ rendering, and more immediately:

I intend to render all the images to storage images that are then going to be modified and presented by compute pipelines.

Not much else to write.
