#include "../headers/the_render.hpp"

namespace the
{
  //the_render will encompass everything that has to do with logic and is found within VulkanEngine/lve_renderer.cpp
  //
  //I will try to not make too much of a muck of it this time, keyword being try.
};
